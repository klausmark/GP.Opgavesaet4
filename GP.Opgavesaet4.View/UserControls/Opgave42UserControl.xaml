<UserControl x:Class="GP.Opgavesaet4.View.UserControls.Opgave42UserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GP.Opgavesaet4.View.UserControls"
             DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=Opgave42UserControlViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">Opgave 4.2 - Dice klassen</TextBlock>
        <Button Grid.Row="1" Width="200" Height="50" Content="Kast terning" Command="{Binding Path=ThrowDiceCommand}"/>
        <TextBlock Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center" Text="{Binding Path=LastDiceResult}"/>
    </Grid>
</UserControl>
